{
  "name": "rebrand",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && yarn run -s serve || yarn run -s dev",
    "build": "preact build --no-prerender",
    "build:pre-render": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "deploy": "gh-pages -d build",
    "yolo": "yarn run build && yarn run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-synacor": "^1.0.0",
    "if-env": "^1.0.0"
  },
  "dependencies": {
    "bulma": "^0.4.2",
    "gh-pages": "^1.0.0",
    "preact": "^8.1.0",
    "preact-cli": "^1.1.2",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.2"
  }
}
